{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install -g pnpm && pnpm install --frozen-lockfile && pnpm run build:prod"
  },
  "deploy": {
    "startCommand": "pnpm run preview || node dist/index.html || serve -s dist",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}